
// Enum yardımıyla sabit değerler listesi tanımlandı ve bir grup sabit değer temsil edildi.
// Böylece yalnızca tanımlı olan değerlerin kullanılmasını sağlanarak hata riski minimuma indirildi.

public enum TransportMethod {
    ROAD, AIR
}
